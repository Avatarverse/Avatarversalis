name: Avatarversalis
author: Aztl
api-version: 1.18
version: ${projectVersion}
main: net.avatarverse.avatarversalis.core.Avatarversalis
softdepend: [PlaceholderAPI]
commands:
  bending:
    aliases: [b, avatarversalis, avatar, av, avs, pk, tla, mtla, bend]
    usage: /<command>
permissions:
  bending.admin:
    default: op