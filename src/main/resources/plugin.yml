name: Avatarversalis
author: Aztl, s3nn
api-version: 1.18
version: ${project.version}
main: net.avatarverse.avatarversalis.Avatarversalis
softdepend: [PlaceholderAPI]
commands:
  bending:
    aliases: [b, avatarversalis, avatar, av, pk, tla]
    usage: /<command>
permissions:
  bending.admin:
    default: op